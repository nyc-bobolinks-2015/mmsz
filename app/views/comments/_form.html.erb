  <%= form_for [@commentable, @comment], id:"comment-form" do |f| %>
  <fieldset>
    <div>
      <%= f.label :body %>
      <%= f.text_area :body %>
    </div>
    <div>
      <%= f.submit %>
    </div>
  </fieldset>
<% end %>
