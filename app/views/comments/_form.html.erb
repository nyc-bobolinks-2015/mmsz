  <%= form_for [@commentable, @comment] do |f| %>
  <fieldset>
    <div>
      <%= f.label :body %>
      <%= f.text_area :body %>
    </div>
    <div>
      <%= f.submit %>
    </div>
  </fieldset>
<% end %>
